
  <view class="list">
  <view>
    <button bindtap="add" size="mini">添加列表</button>
  </view>
    <block  wx:for-items="{{list}}" wx:for-index="index" wx:for-item="item" wx:key="id">
      <view bindtap="tap" class="mylist">
        <text>{{item.id}}</text>: {{item.title}}
      </view>
    </block>
  </view>